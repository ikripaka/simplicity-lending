name: Lint

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'web/**'
  pull_request:
    paths-ignore:
      - 'web/**'
  workflow_call:

permissions:
  contents: read

jobs:
  lint:
    name: Clippy
    runs-on: ubuntu-latest
    env:
      SQLX_OFFLINE: true
      SIMPLEX_COMMIT: "c9cae5da3ace3e502fc19bda9cf5bd3fb909e49b"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust (stable)
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install simplex-cli
        uses: BlockstreamResearch/simplicityhl-toolchain@v0.0.1
        with:
          components: simplex

      - name: Generate contract artifacts
        shell: bash
        run: |
          cd crates/contracts
          simplex install
          simplex build
          test -f src/artifacts/mod.rs

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings
